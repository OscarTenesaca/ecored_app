import 'package:flutter/material.dart';

class PageScanner extends StatelessWidget {
  const PageScanner({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Scanner Page')));
  }
}
